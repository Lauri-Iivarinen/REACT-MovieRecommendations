//easy to change during production/deployment
function Host() {
    //const local = 'http://localhost:8080/'
    const server = 'https://movierecommendations-rest.herokuapp.com/'
    const host = server

    return host
}

export default Host;